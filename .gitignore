# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Skill runtime output — erasure certificates are generated per run
gdpr-audit/

# Third-party corpora are never redistributed with this skill — seed-enron
# fetches from CMU on demand. Guard against a local copy being committed.
enron_mail_*.tar.gz
maildir/

# Evaluation artifacts: the demo answer key from seed-demo, and everything
# derived from the Enron corpus (roster, alias lists, masked copies, labels).
# These name real people and are regenerated on demand.
gdpr-eval/

# Skill runtime artifacts generated by the demo
candidates.json
evaluations.json
direct.json
plan.json
forget-me.sh
*.tmp
